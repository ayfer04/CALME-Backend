services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: calme
      POSTGRES_PASSWORD: calme
      POSTGRES_DB: calme
    ports:
      - "15432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata: